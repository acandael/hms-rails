<section class="publications">
  <header>
    <h1>Output: <%= @category.name %></h1>
  </header>
<% if @publications.any? %>
  <% @publications.each do |year, publications| %>
    <h2><%= year %></h2>
    <% publications.each do |pub| %>
      <p><%= link_to pub.title, publication_path(pub) %></p>
    <% end %>
  <% end %>
<% end %>
</section>
